apiversion: eksctl.io/v1alphas
kind: ClusterConfig

metadata: 
  name: roboshop
  region: us-east-1

manageNodeGroups:
  - name: spot
    instanceType: m5.large
    desiredCapacity: 3  
    spot: true